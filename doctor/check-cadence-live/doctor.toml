description = "I10: every order this pack ships is alive — each orders/*.toml has a live registration (per importing rig when scope=\"rig\", one unbound registration otherwise) and fired within max(3×interval, 15m), read via `gc order history --limit 0` because any positive limit returns city-store rows only; condition-triggered orders (no interval) get the registration arm alone"
