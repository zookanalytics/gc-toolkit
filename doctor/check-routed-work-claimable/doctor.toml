description = "I3: every open bead is reachable by a live actor — a routed unassigned bead's gc.routed_to and an assigned bead's assignee must be byte-identical to a live agent identity or a declared sentinel (the offer/poll is exact string equality), and no scope=\"rig\" order is registered with no rig bound"
