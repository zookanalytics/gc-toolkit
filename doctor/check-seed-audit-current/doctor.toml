description = "Generated-artifact freshness: generated/seed-audit/ matches the source digest of the prompt inputs it renders — a digest mismatch is an error (the committed audit describes a seed no agent receives), while an entirely absent audit (a fresh clone before the first render) is only a warning; assets/scripts/render-seed-audit.sh --check is the authoritative full re-render"
