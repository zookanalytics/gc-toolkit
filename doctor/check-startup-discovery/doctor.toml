description = "Refinery + deacon prompts include four-tier startup discovery (tier-2 routed work bead query, tier-3 open patrol wisp adoption); every patrol-wisp query is ephemeral-aware and title-scoped; boot carries the dedicated mol-deacon-patrol wisp read at both sites (Step 2 and the quick-reference row)"
