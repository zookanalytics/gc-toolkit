description = "Refinery + deacon prompts include four-tier startup discovery (tier-2 routed work bead query, tier-3 open patrol wisp adoption); every patrol-wisp query is ephemeral-aware and title-scoped"
