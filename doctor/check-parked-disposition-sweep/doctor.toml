description = "The parked-disposition sweep stays shipped and wired — assets/scripts/detect-parked-dispositions.sh with its regression test and its origin producer, called from the witness patrol's own step, so a parked operator-origin subject whose routed work has landed cannot go back to waiting for someone to notice a board row (tk-2cyxo)"
