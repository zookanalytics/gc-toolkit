description = "I2: the anchor state space is closed — every open bead's merge_result is a state declared in lifecycle/lifecycle.toml, no OPEN bead carries a closed-only state like merged, and no bead still carries a healer-bookkeeping key the metadata registry deleted with its writer"
