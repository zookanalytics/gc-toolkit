description = "Config/addressing integrity: every agent prompt_template in the RESOLVED city config names a readable file (the unreadable-template path silently primes the 16-line generic stub at exit 0), every fragment named in an inject_fragments/inject_fragments_append list resolves in template-fragments/ (as a file or a {{ define }} block), and every declared overlay_dir exists"
