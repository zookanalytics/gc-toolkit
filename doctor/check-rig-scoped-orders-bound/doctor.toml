description = "A scope=\"rig\" order is never live with no rig bound — order discovery registers this pack's rig-scoped orders at city scope too, where the bare pool cannot be qualified and every fire strands an unclaimable workflow root in the city store (tk-gi2pc). Backstop for the discovery-path guard; permanently green once that lands"
