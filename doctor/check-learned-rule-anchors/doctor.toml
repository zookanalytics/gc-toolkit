description = "The learned-rules prompt surface stays traceable, capped, and fully wired: every bullet in template-fragments/learned-conventions-*.template.md is immediately preceded by a well-formed <!-- rule:… src:… adopted:… --> anchor (and no anchor is left without its bullet), no fragment exceeds the 15-bullet cap that mirrors the distiller's fragment_bullet_cap default, every learned-conventions-<role> fragment that exists is actually injected somewhere (an unwired fragment silently binds nobody), and learned-conventions-polecat appears in BOTH hand-synced sites — pack.toml's polecat patch inject_fragments_append and agents/polecat-codex/agent.toml inject_fragments — retiring the documented hand-sync hazard for this surface; WARNs when any adopted: date is older than 180d, the age signal the challenge pass reads (specs/2026-08-learning-system/implementation-design.md §5–§6; no bead id yet)"
