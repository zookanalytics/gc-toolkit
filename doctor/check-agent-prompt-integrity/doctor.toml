description = "No agent silently renders the 16-line generic stub: every agent.toml whose prompt_template uses the <pack>//<subpath> cross-pack form is surfaced as a warning, because gascity resolves that form against the pack dir (absent under the retired-materialization model) and falls back to the generic stub without failing config load (validator F-03) — confirm manually with gc agent render <name> | wc -l, expect >100"
