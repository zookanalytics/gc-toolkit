description = "No agent silently renders the 16-line generic stub: every agent.toml whose prompt_template uses the <pack>//<subpath> cross-pack form is surfaced as a warning, because gascity resolves that form against the pack dir and, when the path is absent, falls back to the generic stub without failing config load — confirm manually with gc agent render <name> | wc -l, expect >100"
