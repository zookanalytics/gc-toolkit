scope = "city"
wake_mode = "fresh"
work_dir = ".gc/agents/concierge"
nudge = "Check push notifications / open consults, then engage the overseer if they're ready."
idle_timeout = "2h"

[env]
BASH_ENV = "${GT_ROOT}/rigs/gc-toolkit/assets/shell/gc-toolkit-init.sh"
