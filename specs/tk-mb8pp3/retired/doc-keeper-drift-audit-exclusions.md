---
name: doc-keeper-drift-audit-exclusions
description: "Recurring not-drift candidates in the gascity-* briefs that the 24h drift audit re-encounters — already evaluated, do not re-investigate or file."
metadata: 
  node_type: memory
  type: project
  originSessionId: b1589ed8-3ec3-4009-86e0-efe93ab37cca
---

The `mol-doc-keeper-drift-audit` formula (24h cooldown) re-reads `docs/gascity-*.md` every run. DRIFT = a claim made FALSE by a *specific upstream commit* (cite it). NOT drift, so exclude: pre-existing authoring errors (claim was never true — no upstream move caused it) and merely-incomplete claims (that's the sibling `mol-doc-keeper-memory-audit`'s job).

Already-evaluated not-drift candidates (re-skip on future runs; don't redo the archaeology):
- **gascity-agents.md** "reference implementation lives in `agents/{refinery,deacon}/prompt.template.md`" — a *within-pack relative* convention, still valid; those prompts live at `examples/*/packs/*/agents/<name>/prompt.template.md` (same on gascity origin/main + upstream/main). Not a literal repo path that moved.
- **gascity-agents.md** `session_origin` table marking an on-demand named-singleton wake as `ephemeral` — FALSE but pre-existing (named-session materialization stamps `GC_SESSION_ORIGIN=named` for all modes since PR #666, predates the doc). Pre-existing inaccuracy, not drift.
- **gascity-agents.md** keeper-front-door "Lifecycle background" wake table ("work on the hook = durable wake reason") — NOT drift despite the on_demand *idle-sleep* deadlock still being live (`gc-klb7r`, OPEN P1: `compute_awake_set.go` re-suppresses an idle-past-timeout on_demand session because reason "named-demand" isn't in the exempt set). gap#1 (drain-ack gate) was fixed by gascity `02a0fdab0`/#59; gap#2 (idle-sleep, gc-klb7r) is a *tracked operational defect* → excluded from the brief by its own nature gate (defects-with-a-fix live in the bead trail, not the footgun section). origin/main brief is CLEAN; the contested qualifying edit lives only in *unmerged* PR #141, held under BLOCKED bead `tk-z1nkw` (escalated to gc-toolkit.mayor, rework-vs-hold). Treat this whole area as owned/dedup'd — do not re-file or redo the gap#1/gap#2 archaeology.
- **gascity-routing-model.md** line citations `sling_test.go:3809` and `cli.md:2789` — already wrong at the brief's *own* cited survey SHAs (authoring errors); the funcs/rows/behavior still exist. Line shifts ≠ drift unless the citation now points at unrelated code.
- **gascity-reference.md** URLs that 301-redirect but still resolve 200 (e.g. on `docs.gascity.com`: `/internals`→`/reference/index`, `/internals/beads-topology`→`/reference/internal/beads-topology`, `/reference/formula`→`/reference/specs/formula-spec-v2`, `/getting-started/repository-map`→`/getting-started/how-gas-city-works`) — NOT drift: the brief's own Refresh-procedure §1 says "301/302/308 redirects are fine; 404 means the page moved." Only a genuine 404 (no working redirect, like the schema `.json` files in tk-po9jx/#153) is fileable. Host swap `docs.gascityhall.com`→`docs.gascity.com` was its own change (tk-8ny16/#144).
- **Additive command/reconciler changes that touch a documented surface but don't falsify a claim are GAPS, not drift** — even when they hit a command the brief documents. The brief is wrong only if it *claims the old behavior is the whole behavior*; a feature that *adds* a path leaves every existing claim true. Pre-evaluated gascity examples (do not file as drift): `gc session close` gaining an optional no-arg default to `$GC_SESSION_ID` (917195c2c/#13 — the `<id>` form the matrix documents still works); reconciler exempting manual shadow sessions from config-drift drains (441b86d13/#25 — brief makes no config-drift-drain claim); `<pack>//<subpath>` agent path refs (aa89f81e9/#62 — brief's `gc session new` footgun is about qualified-spawn addressing, a different surface). These belong to the sibling memory audit, not this one.

Dedup surface each run = live (non-closed) `doc-update` beads + open PRs of *closed* `doc-update` beads (refinery closes the bead when it opens the mr PR, so bead-only queries miss in-flight PRs). MAX 5 beads/run; the rest re-surface next run. See [[doc-keeper-audit-same-line-overlap]] and [[gascity-agents-doc-source-of-truth]].
