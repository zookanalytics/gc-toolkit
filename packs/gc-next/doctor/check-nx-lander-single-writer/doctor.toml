description = "The lander's max-1 cap is honored in practice — errors when more than one live lander session exists for a rig (a manual `gc session new` mints instances outside the controller's desired-state math and co-owns assigned gating anchors: the duplicate-session footgun). The correctness backstop is the merge skill's single-writer sequence; this check keeps the operational premise true (spec §4)"
